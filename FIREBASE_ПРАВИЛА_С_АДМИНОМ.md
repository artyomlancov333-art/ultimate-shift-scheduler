# 🔒 Правила Firebase для режима администратора

## ⚠️ Важно: Обновите правила Firebase!

После добавления функций редактирования и удаления, нужно обновить правила безопасности в Firebase.

### Шаг 1: Откройте Firebase Console

1. Перейдите на https://console.firebase.google.com/
2. Выберите проект **ultimate-shift-scheduler**
3. Перейдите в **Firestore Database** → **Правила** (Rules)

### Шаг 2: Вставьте новые правила

Скопируйте и вставьте эти правила:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /slots/{slotId} {
      // Все могут читать слоты
      allow read: if true;
      
      // Все могут создавать слоты
      allow create: if true;
      
      // Все могут обновлять и удалять (логика ограничений в приложении)
      // Администратор может редактировать/удалять любые слоты
      // Обычные пользователи могут редактировать/удалять только свои слоты
      allow update: if true;
      allow delete: if true;
    }
    
    match /daysOff/{dayOffId} {
      // Все могут читать выходные дни
      allow read: if true;
      
      // Все могут создавать выходные дни
      allow create: if true;
      
      // Все могут обновлять и удалять (логика ограничений в приложении)
      allow update: if true;
      allow delete: if true;
    }
  }
}
```

### Шаг 3: Опубликуйте правила

Нажмите **"Опубликовать"** (Publish)

---

## 🔐 Как работает безопасность

**В приложении:**
- Администратор (пароль: 9119) может редактировать и удалять **любые** смены
- Обычные пользователи могут редактировать и удалять **только свои** смены
- Логика проверки прав реализована в компоненте `SlotTable.jsx`

**В Firebase:**
- Правила разрешают всем обновлять и удалять (для простоты)
- Реальная защита на уровне приложения (UI показывает кнопки только для разрешённых действий)

---

## ⚠️ Альтернатива: Строгие правила (опционально)

Если хотите добавить защиту на уровне Firebase, можно использовать авторизацию Firebase Auth, но это потребует дополнительной настройки.

Для текущей реализации (с паролем администратора в приложении) правила выше подходят идеально.

---

## ✅ Готово!

После обновления правил:
1. Все функции редактирования и удаления будут работать
2. Администратор сможет управлять всеми сменами
3. Обычные пользователи смогут управлять только своими сменами

