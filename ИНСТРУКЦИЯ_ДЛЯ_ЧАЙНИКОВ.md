# 📖 Простая инструкция для чайников

## Что мы делаем?
Загружаем ваш проект в интернет, чтобы он работал как обычный сайт.

---

## ШАГ 1: Загрузить код на GitHub (как загрузить фото в облако)

### Что такое GitHub?
Это как "облако" для кода. Там хранятся все ваши файлы.

### Как это сделать:

#### 1.1. Создайте аккаунт на GitHub
1. Откройте [github.com](https://github.com)
2. Нажмите **"Sign up"** (Регистрация)
3. Заполните форму и создайте аккаунт

#### 1.2. Создайте новый репозиторий (папку для кода)
1. После входа нажмите зелёную кнопку **"New"** или **"+"** вверху справа
2. В поле **"Repository name"** напишите: `ultimate-shift-scheduler`
3. Выберите **"Public"** (публичный)
4. **НЕ ставьте галочки** на "Add README", "Add .gitignore" и "Choose a license"
5. Нажмите зелёную кнопку **"Create repository"**

#### 1.3. Загрузите файлы через терминал

**Откройте терминал на Mac:**
- Нажмите `Cmd + Пробел`
- Напишите "Terminal" и нажмите Enter

**Скопируйте и вставьте эти команды по очереди:**

```bash
cd /Users/kseniasoboleva/Downloads/ultimate-shift-scheduler
```

Нажмите Enter. Затем:

```bash
git init
```

Нажмите Enter. Затем:

```bash
git add .
```

Нажмите Enter. Затем:

```bash
git commit -m "First commit"
```

Нажмите Enter. Затем (замените YOUR_USERNAME на ваш логин GitHub):

```bash
git remote add origin https://github.com/YOUR_USERNAME/ultimate-shift-scheduler.git
```

Нажмите Enter. Затем:

```bash
git branch -M main
```

Нажмите Enter. Затем:

```bash
git push -u origin main
```

**Вас попросят ввести логин и пароль GitHub.** Введите их.

✅ **Готово!** Ваш код теперь на GitHub.

---

## ШАГ 2: Загрузить сайт на Vercel (сделать сайт доступным в интернете)

### Что такое Vercel?
Это сервис, который превращает ваш код в работающий сайт.

### Как это сделать:

#### 2.1. Зайдите на Vercel
1. Откройте [vercel.com](https://vercel.com)
2. Нажмите **"Sign Up"** (Регистрация)
3. Выберите **"Continue with GitHub"** (Войти через GitHub)
4. Разрешите доступ к вашему GitHub аккаунту

#### 2.2. Создайте новый проект
1. Нажмите большую кнопку **"Add New..."** или **"New Project"**
2. Найдите ваш репозиторий `ultimate-shift-scheduler`
3. Нажмите **"Import"** рядом с ним

#### 2.3. Настройте проект
Vercel сам всё определит, но проверьте:
- **Framework Preset:** должно быть "Vite"
- **Root Directory:** должно быть `./`

**Нажмите кнопку "Deploy"** (Развернуть)

⏳ Подождите 2-3 минуты, пока идёт загрузка...

#### 2.4. Добавьте настройки (важно!)

После деплоя:
1. Нажмите на название проекта
2. Перейдите в **"Settings"** (Настройки) вверху
3. В меню слева выберите **"Environment Variables"** (Переменные окружения)
4. Добавьте 3 переменные (нажмите "Add" для каждой):

**Первая переменная:**
- **Name:** `FIREBASE_API_KEY`
- **Value:** `AIzaSyASJIpNOj58QUKOEEsFYYiqrldmff9Bk50`
- Нажмите "Save"

**Вторая переменная:**
- **Name:** `FIREBASE_AUTH_DOMAIN`
- **Value:** `ultimate-shift-scheduler.firebaseapp.com`
- Нажмите "Save"

**Третья переменная:**
- **Name:** `FIREBASE_PROJECT_ID`
- **Value:** `ultimate-shift-scheduler`
- Нажмите "Save"

5. После добавления всех трёх переменных, перейдите в **"Deployments"** (Развёртывания)
6. Найдите последний деплой и нажмите на три точки **"..."** справа
7. Выберите **"Redeploy"** (Переразвернуть)
8. Подтвердите

✅ **Готово!** У вас теперь есть ссылка на сайт! (Например: `https://ultimate-shift-scheduler.vercel.app`)

---

## ШАГ 3: Настроить Firebase (база данных)

### Что такое Firebase?
Это место, где хранятся все данные вашего приложения (пользователи, слоты и т.д.)

### Как это сделать:

#### 3.1. Зайдите в Firebase
1. Откройте [console.firebase.google.com](https://console.firebase.google.com)
2. Войдите через Google аккаунт

#### 3.2. Создайте или откройте проект
1. Если проекта нет, нажмите **"Add project"** (Добавить проект)
2. Название: `ultimate-shift-scheduler`
3. Нажмите **"Continue"** несколько раз
4. Выберите регион (можно оставить по умолчанию)
5. Нажмите **"Create project"**

#### 3.3. Включите вход по Email/Password
1. В меню слева нажмите **"Authentication"** (Аутентификация)
2. Нажмите **"Get started"** (Начать)
3. Перейдите на вкладку **"Sign-in method"** (Метод входа)
4. Найдите **"Email/Password"** в списке
5. Нажмите на него
6. Переключите **"Enable"** (Включить) в положение ON
7. Нажмите **"Save"** (Сохранить)

#### 3.4. Создайте базу данных Firestore
1. В меню слева нажмите **"Firestore Database"** (База данных Firestore)
2. Нажмите **"Create database"** (Создать базу данных)
3. Выберите **"Start in test mode"** (Начать в тестовом режиме)
4. Нажмите **"Next"**
5. Выберите регион (например, `europe-west`)
6. Нажмите **"Enable"** (Включить)

#### 3.5. Установите правила безопасности
1. В Firestore Database нажмите на вкладку **"Rules"** (Правила) вверху
2. Удалите весь текст в редакторе
3. Скопируйте и вставьте этот текст:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && 
        (request.auth.uid == userId || 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin');
    }
    match /slots/{slotId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow update, delete: if request.auth != null && 
        (resource.data.userId == request.auth.uid ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin');
    }
    match /daysOff/{dayOffId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow delete: if request.auth != null && 
        (resource.data.userId == request.auth.uid ||
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin');
    }
    match /referrals/{referralId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
    }
    match /changeHistory/{historyId} {
      allow read: if request.auth != null;
      allow create: if request.auth != null;
    }
  }
}
```

4. Нажмите **"Publish"** (Опубликовать)

✅ **Готово!** Firebase настроен!

---

## ШАГ 4: Создать первого админа (вас)

### Зачем это нужно?
Чтобы вы могли управлять всеми пользователями через админ-панель.

### Как это сделать:

#### 4.1. Зарегистрируйтесь на сайте
1. Откройте ссылку вашего сайта (из Vercel, например: `https://ultimate-shift-scheduler.vercel.app`)
2. Нажмите **"Зарегистрироваться"** или перейдите на `/register`
3. Заполните форму:
   - Имя
   - Логин
   - Email
   - Пароль
4. Нажмите **"Зарегистрироваться"**

#### 4.2. Найдите ваш ID в Firebase
1. Откройте [console.firebase.google.com](https://console.firebase.google.com)
2. Выберите проект `ultimate-shift-scheduler`
3. Перейдите в **"Firestore Database"**
4. Найдите коллекцию **"users"** (если её нет, она появится после регистрации)
5. Откройте коллекцию **"users"**
6. Найдите документ с вашим email (он создастся автоматически при регистрации)
7. Нажмите на этот документ

#### 4.3. Сделайте себя админом
1. В открывшемся документе найдите поле **"role"**
2. Нажмите на значение `"user"`
3. Измените на `"admin"` (введите вручную)
4. Нажмите **"Update"** (Обновить)

✅ **Готово!** Теперь вы админ!

#### 4.4. Проверьте
1. Обновите страницу сайта (F5)
2. В меню должно появиться **"Админ-панель"** или перейдите на `/admin`
3. Если видите админ-панель — всё работает! 🎉

---

## ШАГ 5: Проверить, что всё работает

### Что проверить:

1. ✅ **Регистрация и вход работают**
   - Зарегистрируйтесь
   - Войдите
   - Выйдите и войдите снова

2. ✅ **Все страницы открываются**
   - Dashboard (главная)
   - Profile (профиль)
   - Team (команда)
   - Leaderboard (рейтинг)
   - Slots (слоты)
   - DaysOff (выходные)

3. ✅ **Админ-панель работает**
   - Войдите как админ
   - Перейдите на `/admin`
   - Проверьте список пользователей
   - Попробуйте создать пользователя

4. ✅ **Можно добавлять слоты**
   - Перейдите на страницу Slots
   - Добавьте тестовый слот
   - Проверьте, что он появился

---

## 🆘 Если что-то не работает

### Проблема: "Ошибка при деплое на Vercel"
**Решение:**
- Проверьте, что все файлы загружены на GitHub
- Посмотрите логи в Vercel (в разделе "Deployments" нажмите на деплой и посмотрите "Logs")

### Проблема: "Не могу войти / регистрация не работает"
**Решение:**
- Проверьте, что в Firebase включён Email/Password в Authentication
- Проверьте, что Firestore создан
- Проверьте правила Firestore (должны быть как в Шаге 3.5)

### Проблема: "Ошибка при загрузке данных"
**Решение:**
- Проверьте переменные окружения в Vercel (Шаг 2.4)
- Убедитесь, что они добавлены правильно
- Переразверните проект (Redeploy)

### Проблема: "Не вижу админ-панель"
**Решение:**
- Убедитесь, что вы изменили `role` на `"admin"` в Firestore
- Обновите страницу (F5)
- Выйдите и войдите снова

---

## 📞 Нужна помощь?

1. Проверьте логи ошибок:
   - В браузере: нажмите F12 → вкладка "Console"
   - В Vercel: Deployments → выберите деплой → Logs
   - В Firebase: Console → смотрите ошибки

2. Убедитесь, что всё сделано по инструкции:
   - ✅ Код на GitHub
   - ✅ Сайт на Vercel
   - ✅ Переменные окружения добавлены
   - ✅ Firebase настроен
   - ✅ Вы админ

---

## ✅ Чеклист готовности

Отметьте галочками, что сделано:

- [ ] Создан аккаунт на GitHub
- [ ] Код загружен на GitHub
- [ ] Создан аккаунт на Vercel
- [ ] Сайт задеплоен на Vercel
- [ ] Добавлены переменные окружения в Vercel
- [ ] Создан проект в Firebase
- [ ] Включён Email/Password в Firebase
- [ ] Создана база Firestore
- [ ] Установлены правила Firestore
- [ ] Зарегистрировались на сайте
- [ ] Сделали себя админом
- [ ] Проверили работу всех страниц

---

## 🎉 Готово!

Если все пункты чеклиста отмечены — ваш сайт полностью работает!

Теперь вы можете:
- Использовать сайт
- Добавлять пользователей через админ-панель
- Интегрировать с Telegram ботом (API готовы)

**Удачи!** 🚀

