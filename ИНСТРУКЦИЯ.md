# 📋 Пошаговая инструкция по запуску

## Шаг 1: Откройте терминал

Откройте терминал (Terminal) на вашем Mac и перейдите в папку проекта:

```bash
cd /Users/alancov333/Desktop/ultimate-shift-scheduler
```

## Шаг 2: Установите зависимости

Выполните команду для установки всех необходимых пакетов:

```bash
npm install
```

Это установит:
- React и React DOM
- Vite (сборщик)
- TailwindCSS (стили)
- Firebase (база данных)

⏱️ Это займёт 1-2 минуты.

## Шаг 3: Настройте Firebase

### 3.1. Создайте проект в Firebase

1. Перейдите на https://console.firebase.google.com/
2. Нажмите "Добавить проект" (Add project)
3. Введите название проекта (например, "shift-scheduler")
4. Следуйте инструкциям мастера

### 3.2. Создайте Firestore базу данных

1. В меню слева выберите **Firestore Database**
2. Нажмите **Создать базу данных** (Create database)
3. Выберите режим: **Начать в тестовом режиме** (Start in test mode)
4. Выберите регион (например, `europe-west1` или ближайший к вам)
5. Нажмите **Готово** (Done)

### 3.3. Получите ключи конфигурации

1. В Firebase Console нажмите на иконку ⚙️ (настройки) рядом с "Project Overview"
2. Выберите **Настройки проекта** (Project settings)
3. Прокрутите вниз до раздела **Ваши приложения** (Your apps)
4. Нажмите на иконку `</>` (Web)
5. Зарегистрируйте приложение (введите любое название, например "Shift Scheduler")
6. Скопируйте объект `firebaseConfig` который выглядит так:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abcdef"
};
```

### 3.4. Вставьте ключи в проект

1. Откройте файл `src/firebase.js` в редакторе
2. Найдите строки 7-14 с `firebaseConfig`
3. Замените значения `YOUR_API_KEY`, `YOUR_AUTH_DOMAIN` и т.д. на реальные значения из Firebase Console

**Пример:**

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyC1234567890abcdefghijklmnop",
  authDomain: "shift-scheduler.firebaseapp.com",
  projectId: "shift-scheduler-12345",
  storageBucket: "shift-scheduler-12345.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef123456"
};
```

## Шаг 4: Запустите приложение

В терминале выполните:

```bash
npm run dev
```

Вы увидите что-то вроде:

```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

## Шаг 5: Откройте в браузере

Откройте браузер и перейдите по адресу:

**http://localhost:5173**

## ✅ Готово! Приложение запущено

Теперь вы можете:

1. **Добавлять слоты:**
   - Выберите сотрудника (Артём, Анастасия, Адель, Ольга, Ксения)
   - Выберите дату
   - Выберите время начала и окончания
   - Нажмите "Добавить слот"

2. **Фильтровать:**
   - Используйте фильтры по имени или дате
   - Нажмите "Сбросить фильтры" чтобы убрать фильтры

3. **Смотреть статистику:**
   - Выберите сотрудника в фильтрах
   - Внизу страницы увидите его часы по дням и общие часы
   - Также отображаются общие часы всех слотов

## 🔧 Полезные команды

- `npm run dev` - запустить dev-сервер
- `npm run build` - собрать проект для продакшена
- `npm run preview` - посмотреть собранную версию

## ⚠️ Важные замечания

1. **Ограничение:** В один временной слот (одна дата + одно время) может быть максимум 2 сотрудника. Если попытаетесь добавить третьего - увидите ошибку.

2. **Real-time обновления:** Все изменения синхронизируются в реальном времени через Firebase. Если откроете приложение в двух вкладках - изменения будут видны сразу.

3. **Остановка сервера:** Нажмите `Ctrl + C` в терминале чтобы остановить dev-сервер.

## 🐛 Если что-то не работает

1. **Ошибка при npm install:**
   - Убедитесь что у вас установлен Node.js (версия 16+)
   - Проверьте: `node --version`

2. **Ошибка Firebase:**
   - Проверьте что все ключи в `firebase.js` заполнены правильно
   - Убедитесь что Firestore создан и работает в тестовом режиме

3. **Приложение не открывается:**
   - Проверьте что сервер запущен (`npm run dev`)
   - Убедитесь что порт 5173 не занят другим приложением

## 📱 Использование

После запуска приложение полностью готово к работе. Все данные сохраняются в Firebase и доступны в реальном времени.

