# 🚀 Загрузка на GitHub и деплой

## Шаг 1: Создайте репозиторий на GitHub

1. Откройте https://github.com/new
2. Название: `ultimate-shift-scheduler`
3. Выберите **Public** или **Private**
4. **НЕ** ставьте галочки (README, .gitignore уже есть)
5. Нажмите **"Create repository"**

## Шаг 2: Загрузите код

Откройте терминал и выполните:

```bash
cd /Users/alancov333/Desktop/ultimate-shift-scheduler

git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/ultimate-shift-scheduler.git
git push -u origin main
```

⚠️ **Замените `ВАШ_USERNAME` на ваш GitHub username!**

## Шаг 3: Деплой на Vercel (самый простой способ)

1. Откройте https://vercel.com
2. Нажмите **"Sign Up"** → войдите через **GitHub**
3. Нажмите **"Add New Project"**
4. Выберите репозиторий `ultimate-shift-scheduler`
5. Нажмите **"Deploy"** (настройки уже правильные)
6. Подождите 1-2 минуты

### ✅ Готово!

Вы получите ссылку вида:
**https://ultimate-shift-scheduler-xxxxx.vercel.app**

---

## ⚠️ Не забудьте:

1. **Настроить Firebase:**
   - Откройте `src/firebase.js`
   - Вставьте ключи из Firebase Console
   - Сделайте коммит и пуш:
   ```bash
   git add src/firebase.js
   git commit -m "Add Firebase config"
   git push
   ```

2. **Создать Firestore Database:**
   - В Firebase Console создайте базу данных
   - Выберите продакшн режим или тестовый

3. **Обновить правила Firebase:**
   - См. файл `FIREBASE_ПРАВИЛА_С_АДМИНОМ.md`

---

## 📝 Обновление кода в будущем

После любых изменений:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Vercel автоматически обновит сайт через 1-2 минуты!

---

**Всё!** Просто загрузите на GitHub и задеплойте на Vercel. 🎉

