# 🔥 Настройка Firebase (после вставки ключей)

## ✅ Что уже сделано:
- Ключи скопированы в `src/firebase.js` ✓

## ⚠️ Что нужно сделать ОБЯЗАТЕЛЬНО:

### Шаг 1: Создайте Firestore Database

1. Откройте https://console.firebase.google.com/
2. Выберите ваш проект **ultimate-shift-scheduler**
3. В меню слева нажмите **Firestore Database**
4. Если база ещё не создана, нажмите **"Создать базу данных"** (Create database)
5. Выберите режим: **"Начать в тестовом режиме"** (Start in test mode)
   - Это разрешит чтение и запись для всех (для тестирования)
6. Выберите регион (например, `europe-west1` или ближайший к вам)
7. Нажмите **"Готово"** (Done)

⏱️ Создание базы займёт 1-2 минуты

### Шаг 2: Настройте правила безопасности (опционально)

После создания базы, перейдите в **Правила** (Rules) и убедитесь что там:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

Это правило разрешает всем читать и писать (для тестирования).

⚠️ **Для продакшена** настройте правила безопасности!

---

## ✅ Что создастся автоматически:

- **Коллекция `slots`** - создастся автоматически при первом добавлении слота
- **Документы** - создадутся автоматически при добавлении слотов через приложение

**Вам НЕ нужно создавать коллекции вручную!** Приложение сделает это само.

---

## 🧪 Проверка работы:

1. Запустите приложение: `npm run dev`
2. Откройте http://localhost:5173
3. Попробуйте добавить слот
4. Если всё работает - данные появятся в Firebase Console → Firestore Database → коллекция `slots`

---

## ❓ Частые проблемы:

**Ошибка: "Firestore has not been initialized"**
→ База данных не создана. Выполните Шаг 1 выше.

**Ошибка: "Missing or insufficient permissions"**
→ Правила безопасности не настроены. Выполните Шаг 2 выше.

**Всё работает локально, но не работает на Vercel**
→ Убедитесь что ключи в `src/firebase.js` правильные и сделайте коммит:
```bash
git add src/firebase.js
git commit -m "Add Firebase config"
git push
```

