# 🚀 Инструкция по деплою приложения

Есть два простых способа разместить приложение в интернете:

## Вариант 1: Vercel (РЕКОМЕНДУЕТСЯ - самый простой) ⭐

### Шаг 1: Загрузите код на GitHub

1. Создайте новый репозиторий на GitHub:
   - Перейдите на https://github.com/new
   - Название: `ultimate-shift-scheduler`
   - Выберите "Public" или "Private"
   - НЕ ставьте галочки (README, .gitignore уже есть)
   - Нажмите "Create repository"

2. В терминале выполните:

```bash
cd /Users/alancov333/Desktop/ultimate-shift-scheduler

# Инициализируйте git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit"

# Добавьте удалённый репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/ultimate-shift-scheduler.git

# Загрузите код
git branch -M main
git push -u origin main
```

### Шаг 2: Деплой на Vercel

1. Перейдите на https://vercel.com/
2. Нажмите **"Sign Up"** и войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий `ultimate-shift-scheduler`
5. Нажмите **"Deploy"** (настройки уже правильные)
6. Подождите 1-2 минуты

### Готово! 🎉

Vercel автоматически даст вам ссылку вида:
**https://ultimate-shift-scheduler.vercel.app**

При каждом обновлении кода на GitHub, Vercel автоматически пересоберёт и обновит сайт!

---

## Вариант 2: GitHub Pages (бесплатный хостинг от GitHub)

### Шаг 1: Загрузите код на GitHub

(См. Шаг 1 из варианта 1)

### Шаг 2: Включите GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Нажмите **Settings** (вверху справа)
3. В меню слева выберите **Pages**
4. В разделе **Source** выберите:
   - Branch: `gh-pages`
   - Folder: `/ (root)`
5. Нажмите **Save**

### Шаг 3: Настройте автоматический деплой

GitHub Actions уже настроен! Просто сделайте коммит:

```bash
git add .
git commit -m "Setup GitHub Pages"
git push
```

GitHub автоматически соберёт проект и задеплоит на GitHub Pages.

### Готово! 🎉

Через 1-2 минуты ваше приложение будет доступно по адресу:
**https://YOUR_USERNAME.github.io/ultimate-shift-scheduler/**

---

## ⚙️ Настройка Firebase для продакшена

После деплоя нужно настроить Firebase:

1. Откройте Firebase Console: https://console.firebase.google.com/
2. Выберите ваш проект
3. Перейдите в **Firestore Database** → **Правила** (Rules)
4. Убедитесь что правила позволяют чтение/запись (для тестирования):

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

⚠️ **Внимание:** Это правило разрешает всем читать и писать. Для продакшена настройте правила безопасности!

5. В Firebase Console → **Настройки проекта** → **Ваши приложения**
6. Добавьте веб-приложение (если ещё не добавили)
7. Скопируйте ключи и вставьте в `src/firebase.js`
8. Сделайте коммит и пуш:

```bash
git add src/firebase.js
git commit -m "Add Firebase config"
git push
```

Vercel/GitHub Pages автоматически пересоберёт проект с новыми настройками.

---

## 📝 Обновление приложения

После любых изменений в коде:

```bash
git add .
git commit -m "Описание изменений"
git push
```

Сайт автоматически обновится через 1-2 минуты!

---

## 🔗 Полезные ссылки

- **Vercel:** https://vercel.com
- **GitHub:** https://github.com
- **Firebase Console:** https://console.firebase.google.com

---

## ❓ Частые вопросы

**Q: Какой вариант выбрать?**
A: Vercel проще и быстрее настраивается. GitHub Pages бесплатный, но требует немного больше шагов.

**Q: Нужно ли платить?**
A: Нет! Оба варианта бесплатные для личных проектов.

**Q: Можно ли изменить домен?**
A: Да, в Vercel можно подключить свой домен. В GitHub Pages тоже можно (через настройки репозитория).

