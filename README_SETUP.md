# Ultimate Shift Scheduler - Полная документация

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

### Сборка для продакшена

```bash
npm run build
```

## 📁 Структура проекта

```
src/
├── components/          # React компоненты
│   ├── Layout.tsx      # Основной layout с навигацией
│   ├── ProtectedRoute.tsx  # Защита маршрутов
│   └── ThemeToggle.tsx     # Переключатель темы
├── contexts/           # React Contexts
│   └── AuthContext.tsx     # Контекст аутентификации
├── hooks/              # Custom hooks
│   ├── useUserData.ts      # Хук для данных пользователя
│   └── useAdmin.ts         # Хук для админ-функций
├── pages/              # Страницы приложения
│   ├── Dashboard.tsx       # Главная страница пользователя
│   ├── Profile.tsx         # Профиль пользователя
│   ├── Team.tsx            # Команда и рефералы
│   ├── Leaderboard.tsx    # Рейтинг
│   ├── Slots.tsx           # Управление слотами
│   ├── DaysOff.tsx         # Выходные дни
│   └── admin/              # Админ-панель
│       ├── AdminDashboard.tsx
│       ├── AdminUsers.tsx
│       ├── AdminUserDetail.tsx
│       └── AdminCreateUser.tsx
├── services/           # Бизнес-логика
│   ├── firestore.ts        # Работа с Firestore
│   ├── rating.ts           # Расчёт рейтинга
│   └── referrals.ts        # Реферальная система
├── types/              # TypeScript типы
│   └── index.ts
└── config/             # Конфигурация
    └── firebase.ts         # Настройка Firebase

api/                    # API endpoints для Vercel
├── bot/                # Telegram Bot API
│   ├── set-earnings.ts
│   ├── get-profile.ts
│   ├── get-team.ts
│   └── get-rating.ts
└── cron/               # Cron jobs
    └── recalculate-ratings.ts
```

## 🔐 Аутентификация

Приложение использует Firebase Authentication:
- Email/Password авторизация
- Автоматическое создание записи в Firestore при регистрации
- Защита маршрутов через `ProtectedRoute`

## 📊 Система рейтинга

### Формула рейтинга

```
Рейтинг = (E * 0.6 + T * 0.3 + N * 10) * A * Boost
```

Где:
- **E** - заработок (earnings)
- **T** - командный заработок (teamEarnings)
- **N** - количество приглашённых (invitedCount)
- **A** - активность (activity)
- **Boost** - множитель (boost)

### Автоматический пересчёт

Рейтинг пересчитывается автоматически:
1. При изменении заработка
2. При изменении командного заработка
3. При изменении активности
4. При изменении количества приглашённых
5. Раз в сутки через cron job (`/api/cron/recalculate-ratings`)

## 👑 Админ-панель

Доступна по адресу `/admin` для пользователей с ролью `admin`.

### Возможности:

1. **Все пользователи** (`/admin/users`)
   - Таблица всех участников
   - Фильтры по роли, статусу, активности
   - Просмотр места в рейтинге

2. **Просмотр пользователя** (`/admin/users/:id`)
   - Полный просмотр данных пользователя
   - Редактирование полей (заработок, активность, статус, роль)
   - Просмотр слотов, выходных, реферальной структуры
   - История изменений

3. **Создание пользователя** (`/admin/create-user`)
   - Создание пользователя в Firebase Auth
   - Создание записи в Firestore
   - Настройка реферальной связи

## 📱 Мобильная оптимизация

- Адаптивный дизайн для всех экранов
- Bottom navigation на мобильных устройствах
- Sidebar на десктопе
- Оптимизированные таблицы с горизонтальной прокруткой

## 🤖 Telegram Bot API

API endpoints для интеграции с Telegram ботом:

### POST `/api/bot/set-earnings`
Установить заработок пользователя
```json
{
  "userId": "user-id",
  "earnings": 10000
}
```

### POST `/api/bot/get-profile`
Получить профиль пользователя
```json
{
  "userId": "user-id"
}
```

### POST `/api/bot/get-team`
Получить команду пользователя
```json
{
  "userId": "user-id"
}
```

### POST `/api/bot/get-rating`
Получить рейтинг и позицию пользователя
```json
{
  "userId": "user-id"
}
```

## ⏰ Cron Jobs

### Пересчёт рейтинга

Endpoint: `/api/cron/recalculate-ratings`

Настроен в `vercel.json` на запуск раз в сутки (00:00 UTC).

Для защиты можно установить переменную окружения `CRON_SECRET` и передавать её в заголовке:
```
Authorization: Bearer <CRON_SECRET>
```

## 🎨 UI/UX

- Современный дизайн в стиле Web3
- Тёмный режим (автоматическое определение системных настроек)
- Плавные анимации и переходы
- Адаптивная вёрстка
- Без аватаров (полностью удалены)

## 🔧 Переменные окружения

Для продакшена настройте в Vercel:

```
FIREBASE_API_KEY=your-api-key
FIREBASE_AUTH_DOMAIN=your-auth-domain
FIREBASE_PROJECT_ID=your-project-id
CRON_SECRET=your-secret-key (опционально)
```

## 📝 Firebase Collections

- `users` - пользователи
- `slots` - рабочие слоты
- `daysOff` - выходные дни
- `referrals` - реферальные связи
- `changeHistory` - история изменений

## 🚀 Деплой на Vercel

1. Подключите репозиторий к Vercel
2. Настройте переменные окружения
3. Vercel автоматически определит настройки из `vercel.json`
4. API endpoints будут доступны как serverless functions

## 📚 Дополнительная информация

- Все компоненты написаны на TypeScript
- Используется React Router для навигации
- Firebase Firestore для хранения данных
- Tailwind CSS для стилизации
- Vite как сборщик

